
External Requirements:

libxml2
wxWidgets

Lib requirements included.
---- 
>=gsoap-2.7.14
zlib
net-snmp
>=openssl-0.9.8k



Install:


create a system account for the tcanms environment.

adduser -d /opt/tcanms -U -s /sbin/nologin tcanms

source config/environment/$ENV/bashrc

sudo -u $ROLEUSER -H make install


