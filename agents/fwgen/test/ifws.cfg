# rules are process and generated for a single node.

#Zone or location tag = authoritive devices
LOC_A = pulsar : pulsar2
LOC_B = cozmic-gw1 : cozmic-gw2

default_fwtype = iptables  # assume fw is of this type, unless sspecifically set otherwise.
# cisco_acl, cisco_fw, iptables, custom*

pulsar = cisco_acl


# config


LOC_A : eth0 : 10.10.10.0/24 : external


LOC_B : ppp0 : : external
        vmnet1 : 192.168.134.0/24
        vmnet8 : 172.16.176.0/24



